name: dispatcher

channels:
  - pytorch
  - nvidia
  - conda-forge

dependencies:
  - python=3.9
  - pytorch=1.9
  - numpy
  - cudatoolkit
  - psutil
  - pynvml

  - pip
  - pip:
      - fastapi[all]
      - uvicorn[standard]
      - gputil
